services:
  # Backend Web Service (SQLite, ephemeral data)
  - type: web
    name: munna-travels-api
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: CLIENT_URL
        value: https://munna-travels-web.onrender.com
      - key: NODE_ENV
        value: production

  # Frontend Web Service
  - type: web
    name: munna-travels-web
    env: node
    plan: free
    rootDir: frontend
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://munna-travels-api.onrender.com/api
      - key: NODE_ENV
        value: production
